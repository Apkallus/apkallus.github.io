- 信息搜集方式
    - 搜索引擎
    - 搜索引擎的 LLM 模式
    - LLM 聚合搜索
- 在拦截代理服务器的历史记录或选定响应中搜索感兴趣的关键词
    - 页面内容搜索关键词 ```test, admin```
    - 可能关联“未正确实现的访问控制”漏洞
    - 靶场
        - OWASP Juice Shop - Unvalidated Redirects - Outdated Allowlist
            - 已知加密货币功能为过时条目
            - 在 burp 代理历史记录中搜索 ```bitcoin``` 追踪到```main.js``` 文件得到过时条目的重定向信息
        - OWASP Juice Shop - Sensitive Data Exposure - Exposed credentials
            - 已知客户端硬编码了测试账户凭证
            - 在 ```main.js``` 中搜索 ```test``` 得到测试账户凭证
        - OWASP Juice Shop - Broken Access Control - Admin Section
            - 尝试访问管理页面
            - 在 ```main.js``` 中搜索 ```admin``` 得到管理页面路由设置
    - 分析感兴趣的搜索关键词的上下文，确认并使用功能，再进一步搜索
        - 靶场
            - WASP Juice Shop - Security Misconfiguration - Deprecated Interface
                - 已知存在一个未正确关闭的已弃用的 B2B 接口
                - 搜索 ```b2b``` ，上下文位于客户投诉的文件上传功能
                - 描述中包含 b2b 的 XML 格式文件，但文件上传的默认的类型（上传文件时除“所有类型”外的选择框）中不包含
                    “上传区域支持单个发票（PDF格式）或B2B订单文件（XML格式），也可上传包含多个发票/订单的ZIP压缩包”
                - 搜索```zip```或```upload```找到文件上传功能的允许类型
                - 上传```xml```文件触发此接口
- 搜集足量的路径名称来分析开发者的命名模式
- 走“快乐路径”，设置拦截代理服务器被动爬取，访问/使用所有的页面/功能
- 在浏览器的网络选项卡中查看发起者

- 网页注释
    - 以 ```<!--``` 开头的 HTML 注释
    - 在 burp 的站点地图中使用发现注释功能

- 网络服务器管理搜索引擎索引的 ```robots.txt``` 文件
- 应用程序安全政策标准 https://securitytxt.org/ 的 ```security.txt``` 文件
    > 对于网站，security.txt 文件应放置在 /.well-known/ 路径下（ /.well-known/security.txt ）[RFC8615]。它也可以放置在网站的根目录（ /security.txt ）中，特别是当 /.well-known/ 目录因技术原因无法使用时，或者仅仅作为备用方案。该文件可以同时放置在网站的这两个位置。
- 访问资源根目录以确认是否拥有目录索引
    - 靶场
        - OWASP Juice Shop - Sensitive Data Exposure - Confidential Document
        访问其中一个静态文件的路径得到目录

- 阅读对应技术的文档，确定默认端点与设置
    - 文档搜索关键词 ```expect, default```
    - 靶场
        - OWASP Juice Shop - Observability Failures - Exposed Metrics
        浏览官网文档确定 ```prometheus``` 默认端点
    

- 根据用户发布的信息确定或猜测密码或密码问题
    - 靶场
        - OWASP Juice Shop - Sensitive Data Exposure - Meta Geo Stalking
            - 在照片墙中确认照片对应最爱健行地址
            - 下载照片，根据元数据的 GPS 地址找到实际地址
            - 重置密码中的对应问题为最爱健行地址名称
            - 使用地址名称重置密码
        - OWASP Juice Shop - Sensitive Data Exposure - Login MC SafeSearch
            - 搜索歌手找到描述密码的歌曲的歌词
            - 使用歌词中密码的宠物名称与对密码的修改方法确定最终密码
        - OWASP Juice Shop - Sensitive Data Exposure - NFT Takeover
            - 使用在客户评价中泄露的加密货币助记词，转换为钱包私钥后访问

            
- 在过程中构建目录
    - 靶场 
        - OWASP Juice Shop - Sensitive Data Exposure - Security Policy
            - 在之前对计分板 Score Board 的搜索中，在 ```/rest/admin/application-configuration``` 发现的正是 Security 文件 ```http://localhost:3000/.well-known/security.txt``` ，然而未对网站进行结构记录，在进行此挑战时仅在 ```main.js``` 中搜索，最终查看示例解决方案